/**
 * Hybrid flag provider that combines a remote source with a local file fallback,
 * persisting successful remote updates as a snapshot for offline resilience.
 * <p>
 * Entry point: {@link com.openflags.provider.hybrid.HybridFlagProvider#builder()}.
 * </p>
 *
 * @since 0.4.0
 */
package com.openflags.provider.hybrid;
